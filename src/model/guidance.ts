/** Stable model-visible capability summary; secrets deliberately stay outside this surface. */
export const MODEL_MANAGER_GUIDANCE = `Model registration is available through two deliberately separate paths. Use list_model_routes and configure_model_route for primary language/chat models; llm-pi-ai remains their runtime and source of truth. Use list_task_models and register_task_model for image, speech, audio, video, embedding, and reranking catalog entries. A task-model registration is metadata only until a compatible runtime adapter is installed, so never describe registered-only routes as callable. Use select_default_model only for the primary language model used by newly created Agents. Never ask the user to paste an API key into chat and never place a secret in tool arguments or settings. Registration tools store only credential references; direct the user to the secure Settings credential field when a reference is not configured.`
