/** Stable model-visible capability summary; secrets deliberately stay outside this surface. */
export const MODEL_MANAGER_GUIDANCE = `Model provider management is available through list_model_routes, configure_model_route, and select_default_model. Use these tools when the user asks to add, inspect, or choose a primary language model. Never ask the user to paste an API key into chat and never place a secret in tool arguments or settings. configure_model_route stores only a credential reference; direct the user to the secure Models settings field when that reference is not configured. select_default_model affects newly created Agents, while the current session remains on its explicitly selected model.`
